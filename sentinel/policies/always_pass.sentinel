import "strings"
import "time"

print("=================================")
print("Executing always_pass policy")
print("Timestamp: " + strings.to_string(time.now))
print("=================================")

main = rule {
	true
}