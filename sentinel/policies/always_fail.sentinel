import "strings"
import "time"

print("=================================")
print("Executing always_false policy")
print("Timestamp: " + strings.to_string(time.now))
print("=================================")

main = rule {
	false
}